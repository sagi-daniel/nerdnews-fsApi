// import Section from '../../components/Section';
// import Divider from '../../components/Divider';
// import MovieCard from '../../features/movies/MovieCard';
// import MovieModel from '../../models/Movie.model';
// import NewsCard from '../../features/news/NewsCard';
// import NewsModel from '../../models/News.model';

function Search() {
  return (
    <p>ddsd</p>
    // <Section type="vertical" gap="small">
    //   <div className="flex size-full py-10 bg-red-500">
    //     <h2>Hír találatok:</h2>
    //     <div className=" relative py-4 flex justify-center flex-wrap gap-4 ">
    //       {news && news.map((news: NewsModel) => <NewsCard key={news._id} news={news} />)}
    //     </div>
    //   </div>
    //   <Divider margin="full" size="full" />
    //   <div className="flex size-full py-10 bg-red-500">
    //     <h2>Film találatok:</h2>
    //     <div className=" relative py-4 flex justify-center flex-wrap gap-4 ">
    //       {movies && movies.map((movie: MovieModel) => <MovieCard key={movie.id} movie={movie} />)}
    //     </div>
    //   </div>
    // </Section>
  );
}

export default Search;
